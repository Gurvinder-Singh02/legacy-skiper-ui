---
title: Skiper Marquee
description: A Cursor That Follow Images
component: true
links:
---

<ComponentPreview
  name="skiper-marquee-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="All variations"
/>

## Installation

Install the required dependencies.

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli"  >CLI</TabsTrigger>
  <TabsTrigger value="manual" >Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<Steps>

<Step>

Use the CLI to add component to your directory

```bash
  npx shadcn@latest add "http://skiper-ui.com/registry/skiper-marquee.json"
```

</Step>


<Step>

Update tailwind.config.ts

```bash
/** @type {import('tailwindcss').Config} */

module.exports = {

  theme: {
    extend: {
      animation: {
        marquee: "marquee var(--duration) linear infinite",
        "marquee-vertical": "marquee-vertical var(--duration) linear infinite",
      },
      keyframes: {
        marquee: {
          from: { transform: "translateX(0)" },
          to: { transform: "translateX(calc(-100% - var(--gap)))" },
        },
        "marquee-vertical": {
          from: { transform: "translateY(0)" },
          to: { transform: "translateY(calc(-100% - var(--gap)))" },
        },
      },
    },
  },
};
```

</Step>


</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>

Install the required dependencies.

```bash
  npm i framer-motion clsx tailwind-merge
```

</Step>

<Step>
Add util file
```bash
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>

<Step>Copy the source code to `components/ui/skiper-marquee.tsx` </Step>

<ComponentSource name="skiper-marquee" />

<Step>Update the import paths to match your project setup.</Step>

<Step>

Update tailwind.config.ts

```bash
/** @type {import('tailwindcss').Config} */

module.exports = {

  theme: {
    extend: {
      animation: {
        marquee: "marquee var(--duration) linear infinite",
        "marquee-vertical": "marquee-vertical var(--duration) linear infinite",
      },
      keyframes: {
        marquee: {
          from: { transform: "translateX(0)" },
          to: { transform: "translateX(calc(-100% - var(--gap)))" },
        },
        "marquee-vertical": {
          from: { transform: "translateY(0)" },
          to: { transform: "translateY(calc(-100% - var(--gap)))" },
        },
      },
    },
  },
};
```

</Step>

</Steps>



</TabsContent>

</Tabs>

## Usage

```tsx
  <SkiperMarquee />
```