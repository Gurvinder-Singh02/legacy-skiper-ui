---
title: Text Flipper Effect
description: A Cursor That Follow Images
component: true
links:
---

<ComponentPreview
  name="text-effect-flipper-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="All variations"
/>

## Installation

Install the required dependencies.

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<Steps>

<Step>

Paste the below command in your terminal

```bash
  npx shadcn@latest add "http://legacy.skiper-ui.com/registry/text-effect-flipper.json"
```

</Step>

<Step>Update the import paths to match your project setup.</Step>

## Usage

```tsx
import FlipLink from "../ui/text-effect-flipper"
```

```tsx
function BasicExample() {
  return <FlipLink href="https://x.com/guri_who">Behance</FlipLink>
}
```

</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>
Install the required dependencies.
```bash
  npm i framer-motion clsx tailwind-merge
```
</Step>
<Step>
Add util file
```bash
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>

<Step>Copy the source code to `components/ui/text-effect-flipper.tsx` </Step>

<ComponentSource name="text-effect-flipper" />

<Step>Update the import paths to match your project setup.</Step>

## Usage

```tsx
import FlipLink from "../ui/text-effect-flipper"
```

```tsx
function BasicExample() {
  return <FlipLink href="https://x.com/guri_who">Behance</FlipLink>
}
```

</Steps>

</TabsContent>

</Tabs>
