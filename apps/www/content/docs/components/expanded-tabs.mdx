---
title: Expanded Tabs
description: Expanded Tabs usecase
component: true
links:
---

<ComponentPreview
  name="expanded-tabs-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="All variations"
/>

## Installation

Install the required dependencies.

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>
Install the required dependencies.
```bash
  npm i framer-motion clsx tailwind-merge usehooks-ts
```
</Step>
<Step>
Add util file
```bash
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>

<Step>Copy the source code to `components/ui/expanded-tabs.tsx` </Step>

<ComponentSource name="expanded-tabs" />

<Step>Update the import paths to match your project setup.</Step>

## Usage

```tsx
import { ExpandedTabs } from "../ui/expanded-tabs"
```
```tsx

const tabs = [
    { title: "Dashboard", icon: Home },
    { title: "Notifications", icon: Bell },
    { type: "separator" as const },
    { title: "Settings", icon: Settings },
    { title: "Support", icon: HelpCircle },
    { title: "Security", icon: Shield },
  ]
```

```tsx
function BasicExample() {
  return (
     <div className="flex items-center justify-center">
      <ExpandedTabs tabs={tabs} />
      <ExpandedTabs tabs={tabs} activeColor="text-blue-500" className="border-blue-200 dark:border-blue-800" />
    </div>
  )
}
```

</Steps>

</TabsContent>

</Tabs>
