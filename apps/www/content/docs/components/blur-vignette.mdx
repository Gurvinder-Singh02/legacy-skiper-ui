---
title: Blur Vignette
description: A stacked animation with dialog
component: true
links:
---

<ComponentPreview
  name="blur-vignette-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="All variations"
/>

## Installation

Install the required dependencies.

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>
Install the required dependencies.
```bash
  npm i framer-motion clsx tailwind-merge
```
</Step>
<Step>
Add util file
```bash
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>

<Step>Copy the source code to `components/ui/blur-vignette.tsx` </Step>

<ComponentSource name="blur-vignette" />

<Step>Update the import paths to match your project setup.</Step>

## Usage

```tsx
import BlurVignette from "@/components/skiper/BlurVignette"
```

```tsx
function BasicExample() {
  return (
    <BlurVignette
      radius="24px"
      inset="10px"
      transitionLength="100px"
      blur="15px"
      className=" rounded-[2.5rem]"
      switchView={true}
    >
      <video
        autoPlay={true}
        muted
        loop
        content="true"
        className="size-full  object-cover transition-all "
      >
        <source
          src="https://cdn.pixabay.com/video/2023/10/19/185726-876210695_large.mp4"
          type="video/mp4"
        />
      </video>
    </BlurVignette>
  )
}
```

</Steps>

</TabsContent>

</Tabs>
