---
title: Skiper Card
description: A Special Card Component that help you stand out
component: true
links:
---

<ComponentPreview
  name="skiper-card-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="All variations"
/>

<br/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<Steps>

<Step>

Paste the below command in your terminal

```bash
  npx shadcn@latest add "http://skiper-ui.com/registry/skiper-card.json"
```

</Step>


</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>

Install the required dependencies.

```bash
  npm i framer-motion clsx tailwind-merge react-wrap-balancer
```

</Step>

<Step>
Add util file
```bash
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>

<Step>Copy the source code to `components/ui/skiper-card.tsx` </Step>

<ComponentSource name="skiper-card" />

<Step>Update the import paths to match your project setup.</Step>


</Steps>

</TabsContent>



</Tabs>

## Props
| Property     | Description                                                                 |
|--------------|-----------------------------------------------------------------------------|
| title        | The title of the component, e.g., "Components that pop".                   |
| description  | A brief description: "Make your app 🤌".                                   |                                       |
| bgClass      | Configuration for the background styling of the component.                |
| image        | Object containing all image mappings, including light and alt versions.   |
| alt          | The alternate text for images, e.g., "Something".                         |
